name: illegal-dumping-env

environment:
  type: python

entrypoint:
  command: uvicorn main:app --host 0.0.0.0 --port 7860

endpoints:
  reset: /reset
  step: /step
  state: /state

tasks:
  - name: task_easy
    description: Detect garbage
  - name: task_medium
    description: Detect dumping
  - name: task_hard
    description: Predict risk