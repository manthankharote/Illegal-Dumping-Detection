name: illegal-dumping-env

environment:
  type: python

entrypoint:
  command: python inference.py

tasks:
  - id: task_easy
    description: Detect garbage presence

  - id: task_medium
    description: Detect illegal dumping

  - id: task_hard
    description: Predict environmental risk